# Project Frontier — Clean Start

This reset package uses a flat file structure so GitHub and Cloudflare Pages cannot lose nested CSS or JavaScript paths.

Start with `START-HERE.txt`.

Current storage is browser localStorage. Supabase is intentionally not connected until the static deployment is confirmed.
